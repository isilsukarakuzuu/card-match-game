#ifndef SHUFFLE_H
#define SHUFFLE_H

#include <QString>
#include <vector>
#include <filesystem>
#include <fstream>
#include <unordered_set>

std::vector<std::vector<QString>> getShuffledWords(int height, int width);

#endif // SHUFFLE_H
